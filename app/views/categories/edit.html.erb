<div class="container">
  <div class="row">
    <div class="col-md-4"></div>
    
    <div class="col-md-8">
      <%= form_with(model: @category, local: true) do |f| %>
        <%= f.label :name, "カテゴリ名" %>
        <%= f.text_field :name %>
        <%= f.submit "更新" %>
      <% end %>
    </div>
  </div>
</div>